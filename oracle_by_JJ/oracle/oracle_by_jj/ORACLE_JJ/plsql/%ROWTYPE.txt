DECLARE
	
	J EMP%ROWTYPE;
BEGIN
	

	SELECT SAL,DEPTNO,JOB INTO J.SAL, J.DEPTNO,J.JOB FROM EMP
	WHERE ENAME='KING';
	
		
	DBMS_OUTPUT.PUT_LINE(chr(11)||'SAL '||J.SAL||' DEPTNO '||J.DEPTNO||'  JOB  '||J.JOB);
END;