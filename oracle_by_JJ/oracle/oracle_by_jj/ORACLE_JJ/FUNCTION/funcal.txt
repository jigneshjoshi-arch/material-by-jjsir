DECLARE
	ANS NUMBER(2);
	JNAM EMP.ENAME%TYPE;
BEGIN
	JNAM := '&JNAM';
	ANS := JJEMP(JNAM);  -- Function call
	
	IF ANS = 'true' THEN
	DBMS_OUTPUT.PUT_LINE('EMPLOYEE ' ||JNAM ||' EXIST ');
	ELSE
	DBMS_OUTPUT.PUT_LINE('EMPLOYEE ' ||JNAM ||' DOES NOT EXIST ');
	END IF;
END;
