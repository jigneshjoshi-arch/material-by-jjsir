CREATE OR REPLACE FUNCTION JJEMP(JNAM EMP.ENAME%TYPE) RETURN NUMBER IS
	KNAM EMP.ENAME%TYPE;
BEGIN
	SELECT ENAME INTO KNAM
	FROM EMP
	WHERE ENAME = JNAM;
	
	RETURN 1;
EXCEPTION
	WHEN NO_DATA_FOUND THEN
	
	RETURN 0;
END;
