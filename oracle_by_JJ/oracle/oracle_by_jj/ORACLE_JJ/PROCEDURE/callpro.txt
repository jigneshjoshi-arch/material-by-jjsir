DECLARE 
	JSAL EMP.SAL%TYPE;

	JEMPNO EMP.EMPNO%TYPE;
BEGIN
	JEMPNO := &JEMPNO;

	JEMP(JEMPNO,JSAL); 	-- PROCEDURE CALL
	
	IF JSAL IS NOT NULL THEN

	DBMS_OUTPUT.PUT_LINE( ' SALARY IS : ' || JSAL);

	END IF;
END;
