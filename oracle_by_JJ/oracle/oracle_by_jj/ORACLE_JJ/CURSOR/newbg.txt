CREATE TABLE hjd (
    sno NUMBER PRIMARY KEY,
    sname VARCHAR2(50),
    gender CHAR(1),
    stream VARCHAR2(50)
);
CREATE TABLE boys (
    sno NUMBER PRIMARY KEY,
    sname VARCHAR2(50),
    gender CHAR(1),
    stream VARCHAR2(50)
);
CREATE TABLE girls (
    sno NUMBER PRIMARY KEY,
    sname VARCHAR2(50),
    gender CHAR(1),
    stream VARCHAR2(50)
);