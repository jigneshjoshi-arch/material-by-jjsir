create or replace package jpack as
	procedure multi(n1 number, n2 number);
end jpack;
/

create or replace package body jpack as

	procedure multi(n1 in number, n2 in number) is
		ans number(4);
	begin
		ans := n1 * n2;
		dbms_output.put_line('multiplication is '||ans);
	end multi;
end jpack;
